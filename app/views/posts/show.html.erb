<h1><%= @post.title %></h1>
<p><%= @post.content %></p>
<%= button_to 'Delete', post_path(@post), method: :delete, class: 'delete-link', data: { confirm: 'Are you sure?' } %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

